class TreeNode
{
  public:
    TreeNode* left;
    TreeNode* right;
    int height;
    int key; //which in this example is my data as well

    TreeNode();
    TreeNode(int k);
    virtual ~TreeNode();

    //create front pointer?
};
